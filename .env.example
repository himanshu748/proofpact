# Backend settings. Export these before starting uvicorn; never commit real values.
PAYMENTS_MODE=disabled
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# A Route-linked TEST account. This sandbox uses one operator-configured recipient.
RAZORPAY_LINKED_ACCOUNT_ID=
RAZORPAY_WEBHOOK_SECRET=
# Only rzp_test_ keys are accepted. Live payments are blocked in code.
